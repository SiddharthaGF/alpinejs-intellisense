/**
 * Placeholder for stage 1.
 * Stage 2 introduces the LSP entry point (main.ts) that boots the server,
 * handles lifecycle (initialize, shutdown, exit) and publishes capabilities.
 */
export const LANGUAGE_SERVER_VERSION = "0.0.0" as const;
